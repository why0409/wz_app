<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.safetyHazard.mapper.SafetyHazardUserEvaluateMapper">

    <resultMap type="SafetyHazardUserEvaluate" id="SafetyHazardUserEvaluateResult">
        <result property="id"    column="id"    />
        <result property="userId"    column="user_id"    />
        <result property="content"    column="content"    />
        <result property="annex"    column="annex"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateTime"    column="update_time"    />
    </resultMap>

    <sql id="selectSafetyHazardUserEvaluateVo">
        select id, user_id, content, annex, create_time, update_time from safety_hazard_user_evaluate
    </sql>

    <select id="selectSafetyHazardUserEvaluateList" parameterType="SafetyHazardUserEvaluate" resultMap="SafetyHazardUserEvaluateResult">
        <include refid="selectSafetyHazardUserEvaluateVo"/>
        <where>
            <if test="userId != null "> and user_id = #{userId}</if>
            <if test="content != null  and content != ''"> and content = #{content}</if>
            <if test="annex != null  and annex != ''"> and annex = #{annex}</if>
        </where>
    </select>

    <select id="selectSafetyHazardUserEvaluateById" parameterType="Long" resultMap="SafetyHazardUserEvaluateResult">
        <include refid="selectSafetyHazardUserEvaluateVo"/>
        where id = #{id}
    </select>

    <insert id="insertSafetyHazardUserEvaluate" parameterType="SafetyHazardUserEvaluate" useGeneratedKeys="true" keyProperty="id">
        insert into safety_hazard_user_evaluate
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="userId != null">user_id,</if>
            <if test="content != null">content,</if>
            <if test="annex != null">annex,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateTime != null">update_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="userId != null">#{userId},</if>
            <if test="content != null">#{content},</if>
            <if test="annex != null">#{annex},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateTime != null">#{updateTime},</if>
        </trim>
    </insert>

    <update id="updateSafetyHazardUserEvaluate" parameterType="SafetyHazardUserEvaluate">
        update safety_hazard_user_evaluate
        <trim prefix="SET" suffixOverrides=",">
            <if test="userId != null">user_id = #{userId},</if>
            <if test="content != null">content = #{content},</if>
            <if test="annex != null">annex = #{annex},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </trim>
        where id = #{id}
    </update>

    <delete id="deleteSafetyHazardUserEvaluateById" parameterType="Long">
        delete from safety_hazard_user_evaluate where id = #{id}
    </delete>

    <delete id="deleteSafetyHazardUserEvaluateByIds" parameterType="String">
        delete from safety_hazard_user_evaluate where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>
